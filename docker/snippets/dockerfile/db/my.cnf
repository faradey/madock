[client]
default-character-set=utf8mb4

[mysql]
default-character-set=utf8mb4

[mysqld]
innodb_file_per_table = 1
collation-server = utf8mb4_unicode_ci
init-connect='SET NAMES utf8mb4'
character-set-server = utf8mb4
wait_timeout=30000
innodb_buffer_pool_size = {{{memShare .db.memory 2 3 "M"}}}
innodb_log_buffer_size = {{{memShare .db.memory 1 3 "M"}}}
innodb_write_io_threads = 16
innodb_flush_log_at_trx_commit = 0
log_bin_trust_function_creators = 1
max_allowed_packet = 64M

{{{- if .logs.persist.enabled}}}
# The error log leaves stderr entirely, which is the one place this feature
# trades something away: MariaDB writes to a file or to stderr, not to both. It
# is worth it — the stream is deleted with the container, and `madock logs db`
# was the only place these lines existed.
log_error = /var/log/madock/mysql-error.log

# And the slow query log, which was never configured at all. `log_slow_query`,
# `general_log` and `log_error` were all commented out in the shipped config, so
# a slow query on any madock project has never been recorded anywhere. Two
# seconds is the threshold a person would set by hand on the day they finally
# needed it.
slow_query_log = 1
slow_query_log_file = /var/log/madock/mysql-slow.log
long_query_time = 2
{{{- end}}}

sql_mode=NO_ENGINE_SUBSTITUTION,STRICT_TRANS_TABLES
