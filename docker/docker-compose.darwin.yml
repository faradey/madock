version: "3.7"

services:
  php:
    volumes:
      - ./src/pub/media/:/var/www/html/pub/media/
      - filesdata:/var/www/html
      #- composercache:/var/www/html/var/composer_home

  nginx:
    volumes:
      - ./src/pub/media/:/var/www/html/pub/media/
      - filesdata:/var/www/html
      - ./ctx/nginx.conf:/etc/nginx/conf.d/default.conf:delegated

volumes:
  filesdata:
  #composercache:
    #external: true