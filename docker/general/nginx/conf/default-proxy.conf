upstream upstreamm_madock_{{{port/nginx}}} {
  keepalive 512;
  server {{{main_upstream_server}}} max_fails=3 fail_timeout=30s;
}
upstream upstreamm_madock_{{{port/livereload}}} {
  server host.docker.internal:{{{port/livereload}}} max_fails=3 fail_timeout=30s;
}
upstream upstreamm_madock_{{{port/vite}}} {
  server host.docker.internal:{{{port/vite}}} max_fails=3 fail_timeout=30s;
}

# This is required to proxy Grafana Live WebSocket connections.
map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

{{{proxy/rate_limit/zone}}}

{{{proxy/gzip/block}}}

# Access log format
log_format main '$remote_addr - $host [$time_local] "$request" '
                '$status $body_bytes_sent "$http_referer" '
                '"$http_user_agent" $request_time';

access_log /var/log/nginx/access.log main;

server {
        listen {{{nginx/port/unsecure}}};
        server_name  {{{nginx/host_names}}};
        location / {
            {{{proxy/rate_limit/req}}}
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   Host      $http_host;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            client_max_body_size       2G;
            client_body_buffer_size    512k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;
            send_timeout 60s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
            proxy_pass         http://upstreamm_madock_{{{port/nginx}}};
        }

        location /phpmyadmin/ {
            proxy_pass         http://{{{container/phpmyadmin}}}:80/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        location /phpmyadmin2/ {
            proxy_pass         http://{{{container/phpmyadmin2}}}:80/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        location /kibana/ {
            proxy_pass         http://{{{container/kibana}}}:5601/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        location /opensearch-dashboard/ {
            proxy_pass         http://{{{container/opensearchdashboard}}}:5601/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }
<<<if{{{magento/mftf/enabled}}}>>>
        location /mftf-selenium/ {
            proxy_pass         http://{{{container/selenium}}}:7900/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        location /websockify {
              proxy_pass http://{{{container/selenium}}}:7900/;
              proxy_http_version 1.1;
              proxy_set_header Upgrade $http_upgrade;
              proxy_set_header Connection "Upgrade";
              proxy_set_header Host $host;
            }
        <<<endif>>>

        location /grafana/ {
            proxy_pass         http://{{{container/grafana}}}:3000/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        # Proxy Grafana Live WebSocket connections.
      location /grafana/api/live/ {
        rewrite  ^/grafana/(.*)  /$1 break;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_pass http://{{{container/grafana}}}:3000;
      }
}

server {
        listen {{{nginx/port/secure}}} ssl {{{nginx/http/version}}};
        server_name  {{{nginx/host_names}}};
        location / {
            {{{proxy/rate_limit/req}}}
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   Host      $host;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            client_max_body_size       2G;
            client_body_buffer_size    512k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;
            send_timeout 60s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
            proxy_pass         http://upstreamm_madock_{{{port/nginx}}};
        }

        location /phpmyadmin/ {
            proxy_pass         http://{{{container/phpmyadmin}}}:80/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        location /phpmyadmin2/ {
            proxy_pass         http://{{{container/phpmyadmin2}}}:80/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        location /kibana/ {
            proxy_pass         http://{{{container/kibana}}}:5601/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        location /opensearch-dashboard/ {
            proxy_pass         http://{{{container/opensearchdashboard}}}:5601/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }
<<<if{{{magento/mftf/enabled}}}>>>
        location /mftf-selenium/ {
            proxy_pass         http://{{{container/selenium}}}:7900/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        location /websockify {
              proxy_pass http://{{{container/selenium}}}:7900/;
              proxy_http_version 1.1;
              proxy_set_header Upgrade $http_upgrade;
              proxy_set_header Connection "Upgrade";
              proxy_set_header Host $host;
            }
        <<<endif>>>

        location /grafana/ {
            proxy_pass         http://{{{container/grafana}}}:3000/;
            proxy_redirect     off;

            proxy_set_header   Host             $host;
            proxy_set_header   X-Real-IP        $remote_addr;
            proxy_set_header   X-Forwarded-For  $proxy_add_x_forwarded_for;

            client_max_body_size       2G;
            client_body_buffer_size    256k;

            proxy_connect_timeout      {{{proxy/timeout/connect}}}s;
            proxy_send_timeout         {{{proxy/timeout/send}}}s;
            proxy_read_timeout         {{{proxy/timeout/read}}}s;

            proxy_buffer_size          256k;
            proxy_buffers              256 16k;
            proxy_busy_buffers_size    256k;
            proxy_temp_file_write_size 256k;
        }

        # Proxy Grafana Live WebSocket connections.
          location /grafana/api/live/ {
            rewrite  ^/grafana/(.*)  /$1 break;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            proxy_set_header Host $host;
            proxy_pass http://{{{container/grafana}}}:3000;
          }

        ssl_certificate /sslcert/fullchain.crt;
        ssl_certificate_key /sslcert/madock.local.key;
        include /sslcert/options-ssl-nginx.conf;
}

server {
        listen 35729;
        server_name  {{{nginx/host_names}}};
        location / {
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_read_timeout 86400;
            proxy_pass http://upstreamm_madock_{{{port/livereload}}}/;
        }
}

server {
        listen 5173;
        server_name  {{{nginx/host_names}}};
        location / {
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_read_timeout 86400;
            proxy_pass http://upstreamm_madock_{{{port/vite}}}/;
        }
}
