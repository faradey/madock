version: "3.7"
services:
  php:
    container_name: {{{PROJECT_NAME}}}-php-1
    build:
      context: ctx
      dockerfile: php.Dockerfile
    volumes: &appvolumes
      - ./ctx/nginx.conf:/etc/nginx/conf.d/default.conf:delegated
    extra_hosts:
      - "host.docker.internal:host-gateway"

  nginx:
    container_name: {{{PROJECT_NAME}}}-nginx-1
    build:
      context: ctx
      dockerfile: nginx.Dockerfile
    ports:
      - "{{{NGINX_PROJECT_PORT}}}:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - php

  db:
    container_name: {{{PROJECT_NAME}}}-db-1
    command: mysqld --default-authentication-plugin=mysql_native_password
    build:
      context: ctx
      dockerfile: db.Dockerfile
    volumes:
      - dbdata:/var/lib/mysql
      - ./ctx/my.cnf:/etc/mysql/conf.d/mysql.cnf:delegated

  node:
    container_name: {{{PROJECT_NAME}}}-node-1
    build:
      context: ctx
      dockerfile: node.Dockerfile
    ports:
      - 35729:35729
    volumes: *appvolumes
    profiles:
      - node{{{NODEJS_ENABLE}}}
    depends_on:
      - php

  elasticsearch:
    container_name: {{{PROJECT_NAME}}}-elasticsearch-1
    build:
      context: ctx
      dockerfile: elasticsearch.Dockerfile
    deploy:
      resources:
        limits:
          memory: 2512m
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    environment:
      - discovery.type=single-node
      - "ES_JAVA_OPTS=-Xms800m -Xmx800m"
    volumes:
      - elasticsearch_vlm:/usr/share/elasticsearch/data
    profiles:
      - elasticsearch{{{ELASTICSEARCH_ENABLE}}}

  redisdb:
    container_name: {{{PROJECT_NAME}}}-redisdb-1
    build:
      context: ctx
      dockerfile: redis.Dockerfile
    profiles:
      - redisdb{{{REDIS_ENABLE}}}

  rabbitmq:
    container_name: {{{PROJECT_NAME}}}-rabbitmq-1
    image: rabbitmq:{{{RABBITMQ_VERSION}}}
    profiles:
      - rabbitmq{{{RABBITMQ_ENABLE}}}

  phpmyadmin:
    container_name: {{{PROJECT_NAME}}}-phpmyadmin-1
    image: phpmyadmin/phpmyadmin:latest
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_ARBITRARY: 1
      PMA_ABSOLUTE_URI: "http://{{{HOST_NAME_DEFAULT}}}/phpmyadmin/"
    ports:
      - "{{{NGINX_PROJECT_PORT+1}}}:80"
    depends_on:
      - db

volumes:
  dbdata:
  elasticsearch_vlm:
