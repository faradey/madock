version: "3.7"
services:
  php:
    build:
      dockerfile: php.Dockerfile
    volumes: &appvolumes
      - ../../.composer:/var/www/.composer:delegated
      - ../../.composer:/var/www/html/var/composer_home:delegated
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      app_net_{{{NETWORK_NUMBER}}}:

  nginx:
    build:
      dockerfile: nginx.Dockerfile
    volumes: *appvolumes
      - ./ctx/nginx.conf:/etc/nginx/conf.d/default.conf:delegated
    ports:
      - "{{{NGINX_PROJECT_PORT}}}:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - php
    networks:
      app_net_{{{NETWORK_NUMBER}}}:

  db:
    command: mysqld --default-authentication-plugin=mysql_native_password
    build:
      dockerfile: db.Dockerfile
    volumes:
      - ./data/mysql/:/var/lib/mysql:delegated
      - ./ctx/my.cnf:/etc/mysql/conf.d/mysql.cnf:delegated
    networks:
      app_net_{{{NETWORK_NUMBER}}}:

  node:
    build:
      dockerfile: node.Dockerfile
    ports:
      - 35729:35729
    volumes: *appvolumes
    profiles:
      - node{{{NODEJS_ENABLE}}}
    depends_on:
      - php

  elasticsearch:
    build:
      dockerfile: elasticsearch.Dockerfile
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    environment:
      - discovery.type=single-node
    volumes:
      - elasticsearch_vlm:/usr/share/elasticsearch/data
    profiles:
      - elasticsearch{{{ELASTICSEARCH_ENABLE}}}
    networks:
      app_net_{{{NETWORK_NUMBER}}}:

  redisdb:
    build:
      dockerfile: redis.Dockerfile
    profiles:
      - redisdb{{{REDIS_ENABLE}}}
    networks:
      app_net_{{{NETWORK_NUMBER}}}:

  rabbitmq:
    image: rabbitmq:{{{RABBITMQ_VERSION}}}
    profiles:
      - rabbitmq{{{RABBITMQ_ENABLE}}}
    networks:
      app_net_{{{NETWORK_NUMBER}}}:

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_ARBITRARY: 1
      PMA_ABSOLUTE_URI: "http://{{{HOST_NAME_DEFAULT}}}/phpmyadmin/"
    ports:
      - "{{{NGINX_PROJECT_PORT+1}}}:80"
    depends_on:
      - db
    networks:
      app_net_{{{NETWORK_NUMBER}}}:

volumes:
  elasticsearch_vlm:
networks:
  app_net_{{{NETWORK_NUMBER}}}:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.21.{{{NETWORK_NUMBER}}}.0/24
